# my_lab

Minimal Python project scaffold created by assistant.

Quick start

- Create a virtual environment: `python -m venv .venv`
- Activate it: `source .venv/bin/activate`
- Install dev deps: `pip install -r requirements.txt`
- Run tests: `pytest`
